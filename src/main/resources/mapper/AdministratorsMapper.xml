<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyg.mall.mapper.AdministratorsMapper">
    <select id="list" resultType="Administrators" parameterType="String">
        select * from administrators WHERE name = #{0} and password = #{1}
    </select>
</mapper>